{
  "name": "ghl-referral-webhook",
  "version": "1.0.0",
  "description": "Node.js webhook server for incrementing GoHighLevel contact referral counts - Vercel optimized",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "vercel-dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "express-validator": "^7.0.1",
    "axios": "^1.6.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}